An image classifier which uses a subset of the Flowers17 database.
The classification is done using transfer learning with PyTorch and ResNet as the base network.

For more information see the credits document